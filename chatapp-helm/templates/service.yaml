apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatapp-helm.fullname" . }}
  labels:
    {{- include "chatapp-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatapp-helm.selectorLabels" . | nindent 4 }}
